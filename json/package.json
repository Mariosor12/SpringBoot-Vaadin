{  
  "name": "json-server",  
  "version": "1.0.0",  
  "scripts": {  
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT",  
    "vercel-build": "echo 'No build step necessary'"  
  },  
  "dependencies": {  
    "json-server": "^0.17.0"  
  }  
}